<%#
  Partial: Sub-Navigation Tabs (Rama 1)
  Tabs: Tax Sale Auctions
  NOTE: Prior Sale Results tab hidden per client decision — not selling this data for now
%>
<div class="r1-sub-nav">
  <a href="<%= research_auctions_path %>" class="r1-sub-tab active" id="tab-taxsale">
    <i data-lucide="gavel" style="width:14px;height:14px;margin-right:4px;"></i> Tax Sale Auctions
  </a>
  <%# Prior Sale Results — HIDDEN per client decision
  <a href="<%= research_auctions_path(sub_tab: 'prior') %>" class="r1-sub-tab" id="tab-prior"
     onclick="r1SwitchSubTab('prior'); return false;">
    <i data-lucide="history" style="width:14px;height:14px;margin-right:4px;"></i> Prior Sale Results
  </a>
  %>
</div>
